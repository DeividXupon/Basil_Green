import styled from "styled-components";
import { corSecundaria, paddingHeight, fontTilt } from "Components/UI/variaveis"

export const BoxCardapio = styled.main`
    margin-top: 70px;
`

export const Titulo = styled.h1`
    color: ${corSecundaria};
    font-family: ${fontTilt};
    padding: 0px ${paddingHeight};
    font-size: 3em;
`